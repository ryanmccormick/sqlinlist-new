import { InputSettingsDirective } from './input-settings.directive';

describe('InputSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSettingsDirective();
    expect(directive).toBeTruthy();
  });
});
